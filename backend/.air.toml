root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/api ./cmd/api"
bin = "./tmp/api"
entrypoint = "./tmp/api"
include_dir = ["cmd", "internal"]
exclude_dir = ["tmp", "openapi"]
delay = 200
stop_on_error = true

[log]
time = true
